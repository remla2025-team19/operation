
- name: Download Helm GPG key (ASCII)
  apt_key:
    url: https://baltocdn.com/helm/signing.asc
    state: present
    

- name: Add Helm APT repository
  apt_repository:
    repo: "deb https://baltocdn.com/helm/stable/debian/ all main"
    filename: helm
    state: present

- name: Install Helm package
  apt:
    name: helm
    state: present
    update_cache: yes
